package get.refer;

import android.app.ListActivity;
import android.os.Bundle;

public class JavaApiList extends ListActivity{

	@Override
	protected void onCreate(Bundle savedInstanceState){
		super.onCreate(savedInstanceState);
		
//		int id = getIntent().getIntExtra("id", 0);
//		String[] dbName = {"java_basic", "arithmetic", "java_advance", "design_pattern", "database", "java_web", "framework", "java_ee", "java_classes", "java_package"};
//		String db = dbName[id];
//		dbName = null;
	}
}
